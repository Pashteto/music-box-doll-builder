# Production build env (committed — NEXT_PUBLIC_API_BASE is a public URL, not a secret).
# Baked into the static export at build time so the browser calls the API subdomain.
NEXT_PUBLIC_API_BASE=https://api.lindentar.pashteto.com
